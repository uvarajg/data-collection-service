# Requirements for Historical Input Data Polygon Script
# Python 3.12+ required

# Core data processing
pandas>=2.0.0
numpy>=1.24.0

# Technical indicators calculation (same as existing service)
ta>=0.10.2

# API and HTTP handling
aiohttp>=3.8.0
ratelimiter>=1.2.0

# Async support
asyncio-throttle>=1.0.2

# Standard library enhancements (if needed)
python-dotenv>=1.0.0

# Logging and monitoring
structlog>=23.0.0

# Data validation and type checking
pydantic>=2.0.0

# File and path handling (built-in pathlib is sufficient)
# DateTime handling (built-in datetime is sufficient)

# Optional but recommended for production
# uvloop>=0.17.0  # Faster async event loop
# cProfile  # Performance profiling (built-in)